[project]
name = "weather"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JzangTran", email = "caphecungjzang@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=2.0.0",
]

[project.scripts]
weather = "weather:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
